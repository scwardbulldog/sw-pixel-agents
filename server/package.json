{
  "name": "pixel-agents",
  "version": "1.0.0",
  "description": "Standalone agent session monitor with pixel art visualization",
  "bin": {
    "pixel-agents": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.cli.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.2"
  },
  "dependencies": {
    "@types/ws": "^8.18.1",
    "ws": "^8.20.0"
  }
}
